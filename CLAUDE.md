# Zenn記事リポジトリ ルール

## 公開フロー

mainブランチへの直接pushは禁止。記事の新規作成・更新は必ず以下のフローで行う：

1. 作業ブランチを作成する（例: `article/2026-04-25-plugin-ranking`）
2. 記事を作成・編集してコミット
3. PRを作成してユーザーにレビューしてもらう
4. ユーザーの承認後にマージ → Zenn連携で自動公開

## 記事の品質管理

- Web検索の結果をそのまま事実として書かない。CLI実行・GitHubリポジトリ・公式ドキュメント等の一次ソースで必ず検証する
- 検証できない定量データ（インストール数等）は記載しない
- コマンド名・プラグイン名・API名は実際に動作確認してから記載する

## 既存記事の重複防止

記事を作成する前に `articles/` ディレクトリを確認し、同テーマの既存記事がないかチェックする。既存記事がある場合は上書き更新し、新しいファイルを作らない。

## トラブル対応

Zennに誤った記事が公開された場合、gitの操作だけで解決しようとしない。Zennダッシュボード（https://zenn.dev/dashboard）からの手動非公開・削除を最初に提案すること。

## スキル

このリポジトリのスキルは `.claude/skills/<skill-name>/SKILL.md` に配置する（旧 `.claude/commands/` は廃止）。

- `publish-article`: Zennに新規記事を作成してPR経由で公開する
- `research-topic`: AI開発関連トピックを多層リサーチして記事素材を作る
